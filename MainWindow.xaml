<Window x:Class="IndustrialMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IndustrialMonitor"
        xmlns:views="clr-namespace:IndustrialMonitor.Views"
        mc:Ignorable="d"
        DataContext="{Binding MainViewModel, Source={StaticResource Locator}}"
        Title="TIA Portal - Mixing Tank HMI" Height="700" Width="1100" 
        WindowStartupLocation="CenterScreen" Background="#FF1A1F2E">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <views:HeaderView Grid.Row="0"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- P&ID Process Area -->
            <views:ProcessOverview Grid.Column="0"/>

            <!-- Control Panel -->
            <views:ControlPanel Grid.Column="1"/>
        </Grid>

        <!-- Footer / Status Bar -->
        <views:FooterView Grid.Row="2"/>
    </Grid>
</Window>
