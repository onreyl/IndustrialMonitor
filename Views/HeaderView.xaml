<UserControl x:Class="IndustrialMonitor.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="1100">
    
    <Border Background="#FF0D47A1" BorderBrush="#FF1565C0" BorderThickness="0,0,0,2">
        <Grid Margin="15,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Rectangle Width="8" Height="25" Fill="#FF4CAF50" Margin="0,0,12,0"/>
                <TextBlock Text="Mixing Tank Control System" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" FontFamily="Segoe UI"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="PLANT: UNIT-01" Foreground="#AAFFFFFF" FontSize="11" Margin="0,0,20,0"/>
                <TextBlock x:Name="DateTimeText" Text="12.01.2026  22:57:00" Foreground="White" FontSize="12" FontFamily="Consolas"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
